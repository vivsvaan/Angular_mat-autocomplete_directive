import { AutoSelectValueDirective } from './auto-select-value.directive';

describe('AutoSelectValueDirective', () => {
    it('should create an instance', () => {
        const directive = new AutoSelectValueDirective();
        expect(directive).toBeTruthy();
    });
});
